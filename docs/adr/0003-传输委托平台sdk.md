# 0003 传输委托平台 SDK

状态：传输决策保留；未经资料验证的平台能力假设由
[ADR 0006](./0006-显式平台契约与识别器注册.md) 修正。

决定 SDK 不自建 HTTP 通道，传输委托经核验的平台 SDK，避免重复实现批量、重试和
发送生命周期。但仓库目前没有证据证明目标 Furion/UEM 已具备哪些能力，也不能假设
其上下文采集。适配器只管理实例与脚本生命周期；实际调用、字段映射和平台能力由
显式 `PlatformSdkContract` 及其对应版本文档定义。
