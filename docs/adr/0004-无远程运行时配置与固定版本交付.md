# 0004 无远程运行时配置与固定版本交付

引入远程配置中心意味着新的服务依赖与故障面，与"绝不阻断宿主"的底线冲突。决定 SDK 无远程运行时配置，行为纯由各系统本地 init 决定；产物以带版本号的固定文件（如 `event-tracking.v1.0.0.js`）交付，升级由各系统自行切换版本号，不做滚动覆盖。后果是线上没有远程止血能力，因此默认参数必须保守：脱敏默认开启、异常态采集默认关闭、内部错误一律静默。
