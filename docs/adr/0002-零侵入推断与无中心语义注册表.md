# 0002 零侵入推断与无中心语义注册表

上百个系统共享 2-3 套组件库，逐页人工配置埋点语义不可维护，而跨应用的中心语义配置表又会成为发布瓶颈和一致性负担。决定自动采集 = 组件库识别器 + 零侵入推断（就近标题/路由/标识列）打底，推断不准时由集成方在 HTML 上用 `data-track-*` 属性就地覆盖，不建任何中心语义注册表。准确率靠调试面板、`data-track-*` 覆盖与命名 lint 三道保障兜底。
